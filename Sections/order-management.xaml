<Page x:Class="Wpf_Catering_Db_system.Sections.order_management"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wpf_Catering_Db_system.Sections"
      mc:Ignorable="d" 
      d:DesignHeight="860" d:DesignWidth="690"
      Title="order_management" Background="#eff2f7">

    <Grid Focusable="False" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Orders" FontSize="32" FontWeight="SemiBold" Margin="0 70 0 0"/>

        <Button Style="{StaticResource addButton}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Height="30" Width="160" Margin="0 0 200 0" Click="OrdersButton_Click">
            Orders
        </Button>

        <Button Style="{StaticResource addButton}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Height="30" Width="160" Margin="0" Click="DetailedOrdersButton_Click">
            Detailed Orders
        </Button>

        <Frame x:Name="OrdersGrid" Grid.Row="1" NavigationUIVisibility="Hidden" Background="Transparent" Margin="0 20 0 20"/>



    </Grid>
</Page>
